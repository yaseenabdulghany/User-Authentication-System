# User-Authentication-System
A basic user authentication system built with Node.js, Express.js, and MongoDB. Features include user registration and login with validation. Uses Mongoose for database management. Future improvements: password hashing with bcrypt and JWT-based authentication. Simple API for secure user management.
